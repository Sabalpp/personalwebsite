# personalwebsite

Minimal Next.js setup for the personal site with:

- TypeScript
- Tailwind CSS
- shadcn-compatible structure
- `framer-motion` for the handwritten hero treatment

## Structure

- `components/ui` is the default UI component path
- `app/globals.css` is the default global styles entry

## Run

```bash
npm install
npm run dev
```
